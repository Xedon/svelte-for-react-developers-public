# Talk: Svelte for React developer

svelte: created by `pnpm create svelte@latest svelte`
react: created by `pnpm create vite react --template react-ts`

To run this project you can use `pnpm -r install` and `pnpm -r dev``.
